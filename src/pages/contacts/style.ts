import styled from 'styled-components';

const Wrapper = styled.div`
  margin: 0 80px;
  padding-top: 40px;

  h2 {
    margin-bottom: 40px;
  }

  table {
    margin-top: 30px;
  }
`;

export default Wrapper;
